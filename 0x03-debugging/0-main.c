#include "main.h"
/**
 * main - test the function positive_or_negative which should
 * prints either a number is -ve, 0 or +ve
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
